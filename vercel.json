{
  "version": 2,
  "buildCommand": "npm run build",
  "functions": {
    "server.ts": {
      "includeFiles": "dist/**,rss_sources.json,metadata.json"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/server.ts" }
  ]
}
